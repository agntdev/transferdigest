[
  {
    "name": "/subscribe",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Daily summaries aren’t set up yet. Add the bot’s durable storage, then try again."
            }
          }
        ],
        "send": {
          "text": "/subscribe"
        }
      }
    ]
  }
]
